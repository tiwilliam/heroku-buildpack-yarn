#!/usr/bin/env bash

set -x

BUILD_DIR=$1

if [ "${CI}" == "true" ]; then
	# Skip Yarn buildpack on CI
	exit 1
fi

if [ -f "${BUILD_DIR}/yarn.lock" ] && [ -f "${BUILD_DIR}/webpack.config.js" ]; then
  echo "Yarn buildpack"
  exit 0
else
  exit 1
fi
